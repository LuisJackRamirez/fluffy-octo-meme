#include <bits/stdc++.h>
using namespace std;

int
main (void)
{
  priority_queue <pair <int, int>> food;

  food.push (make_pair (3, 3));
  food.push (make_pair (6, 0));
  food.push (make_pair (3, 2));
  food.push (make_pair (2, 1));

  return 0;
}
